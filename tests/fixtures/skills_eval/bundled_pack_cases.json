[
  {
    "id": "bundled_address_pr_comments_normal",
    "workspace": "workspaces/bundled_address_pr_comments",
    "task": "Address every unresolved thread in review_threads.json, make the requested changes, run python checks.py, and write REVIEW_RESPONSES.md mapping T1 and T2 to their resolutions.",
    "invocation_mode": "normal",
    "expected_skills": ["address-pr-comments"],
    "verification_command": "python -c \"import subprocess,sys; from pathlib import Path; subprocess.run([sys.executable,'checks.py'], check=True); text=Path('REVIEW_RESPONSES.md').read_text(); assert 'T1' in text and 'T2' in text\"",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_address_pr_comments_explicit",
    "workspace": "workspaces/bundled_address_pr_comments",
    "task": "Resolve every unresolved thread in review_threads.json, verify the changes with python checks.py, and record a reply-ready mapping in REVIEW_RESPONSES.md.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "address-pr-comments",
    "expected_skills": ["address-pr-comments"],
    "verification_command": "python -c \"import subprocess,sys; from pathlib import Path; subprocess.run([sys.executable,'checks.py'], check=True); text=Path('REVIEW_RESPONSES.md').read_text(); assert 'T1' in text and 'T2' in text\"",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_code_review_normal",
    "workspace": "workspaces/bundled_code_review",
    "task": "Review proposed.diff against the repository instructions and write actionable severity-ordered findings with exact file and line references to REVIEW.md. For a faulty conditional, include a concrete replacement or guard condition.",
    "invocation_mode": "normal",
    "expected_skills": ["code-review"],
    "verification_command": "python -c \"import re; from pathlib import Path; text=Path('REVIEW.md').read_text(); lower=text.lower(); assert re.search(r'(?:src/)?calculator[.]py:2\\b', lower); assert re.search(r'count\\s*(?:<=|==)\\s*0', lower)\"",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_code_review_explicit",
    "workspace": "workspaces/bundled_code_review",
    "task": "Assess proposed.diff for correctness, tests, security, and local conventions, then write severity-ordered file-and-line findings to REVIEW.md. For a faulty conditional, include a concrete replacement or guard condition.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "code-review",
    "expected_skills": ["code-review"],
    "verification_command": "python -c \"import re; from pathlib import Path; text=Path('REVIEW.md').read_text(); lower=text.lower(); assert re.search(r'(?:src/)?calculator[.]py:2\\b', lower); assert re.search(r'count\\s*(?:<=|==)\\s*0', lower)\"",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_commit_normal",
    "workspace": "workspaces/bundled_commit",
    "task": "Run sh prepare_repo.sh, inspect only what it stages, and create one accurate Conventional Commit. Do not stage anything else, amend, or push.",
    "invocation_mode": "normal",
    "expected_skills": ["commit"],
    "verification_command": "python -c \"import subprocess; s=subprocess.check_output(['git','log','-1','--pretty=%s'], text=True).strip(); t=s.split(': ',1)[0].split('(',1)[0].rstrip('!'); paths=set(subprocess.check_output(['git','show','--pretty=','--name-only','HEAD'], text=True).splitlines()); count=int(subprocess.check_output(['git','rev-list','--count','HEAD'], text=True).strip()); content=subprocess.check_output(['git','show','HEAD:settings.toml'], text=True); assert ': ' in s and t in {'feat','fix','docs','refactor','test','chore','build','ci','perf'} and paths == {'settings.toml'} and count == 2 and content == 'retry_limit = 3\\n'\"",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_commit_explicit",
    "workspace": "workspaces/bundled_commit",
    "task": "Prepare the fixture with sh prepare_repo.sh, inspect the staged diff, and create exactly one Conventional Commit without amending or pushing.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "commit",
    "expected_skills": ["commit"],
    "verification_command": "python -c \"import subprocess; s=subprocess.check_output(['git','log','-1','--pretty=%s'], text=True).strip(); t=s.split(': ',1)[0].split('(',1)[0].rstrip('!'); paths=set(subprocess.check_output(['git','show','--pretty=','--name-only','HEAD'], text=True).splitlines()); count=int(subprocess.check_output(['git','rev-list','--count','HEAD'], text=True).strip()); content=subprocess.check_output(['git','show','HEAD:settings.toml'], text=True); assert ': ' in s and t in {'feat','fix','docs','refactor','test','chore','build','ci','perf'} and paths == {'settings.toml'} and count == 2 and content == 'retry_limit = 3\\n'\"",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_commit_unstaged_normal",
    "workspace": "workspaces/bundled_commit_unstaged",
    "task": "Run sh prepare_repo.sh, then commit my changes with an accurate message; skip anything that is obviously scratch output. Do not amend or push.",
    "invocation_mode": "normal",
    "expected_skills": ["commit"],
    "verification_command": "python -c \"import subprocess; s=subprocess.check_output(['git','log','-1','--pretty=%s'], text=True).strip(); t=s.split(': ',1)[0].split('(',1)[0].rstrip('!'); paths=set(subprocess.check_output(['git','show','--pretty=','--name-only','HEAD'], text=True).splitlines()); count=int(subprocess.check_output(['git','rev-list','--count','HEAD'], text=True).strip()); assert ': ' in s and t in {'feat','fix','docs','refactor','test','chore','build','ci','perf'} and 'settings.toml' in paths and 'debug.log' not in paths and count == 2\"",
    "tags": ["bundled-pack", "positive", "unstaged"]
  },
  {
    "id": "bundled_debug_normal",
    "workspace": "workspaces/bundled_debug",
    "task": "python repro.py raises ValueError for an empty count even though the expected result is zero. Reproduce it, isolate the root cause, make the smallest fix, and verify the original command.",
    "invocation_mode": "normal",
    "expected_skills": ["debug"],
    "verification_command": "python repro.py",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_debug_explicit",
    "workspace": "workspaces/bundled_debug",
    "task": "Reproduce the ValueError from python repro.py, isolate and fix its cause, then verify that exact symptom is gone.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "debug",
    "expected_skills": ["debug"],
    "verification_command": "python repro.py",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_fix_ci_normal",
    "workspace": "workspaces/bundled_fix_ci",
    "task": "The build's slug check is failing. Inspect ci-output.txt, reproduce the failing command locally, fix the evidenced cause, and rerun only the affected check.",
    "invocation_mode": "normal",
    "expected_skills": ["fix-ci"],
    "verification_command": "python ci_check.py",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_fix_ci_explicit",
    "workspace": "workspaces/bundled_fix_ci",
    "task": "Use ci-output.txt to reproduce the failed slug job, implement the smallest repair, and rerun python ci_check.py without weakening the check.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "fix-ci",
    "expected_skills": ["fix-ci"],
    "verification_command": "python ci_check.py",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_release_notes_normal",
    "workspace": "workspaces/bundled_release_notes",
    "task": "Draft RELEASE_NOTES.md for v1.5.0 from the merged history since v1.4.0 exported in history.txt. Group user-visible changes and call out the breaking change.",
    "invocation_mode": "normal",
    "expected_skills": ["release-notes"],
    "verification_command": "python -c \"from pathlib import Path; text=Path('RELEASE_NOTES.md').read_text(); required=('quoted fields','exit status','legacy --old'); assert all(item in text for item in required)\"",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_release_notes_explicit",
    "workspace": "workspaces/bundled_release_notes",
    "task": "Turn history.txt into grouped v1.5.0 notes in RELEASE_NOTES.md, including the breaking removal and the revision range starting at v1.4.0.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "release-notes",
    "expected_skills": ["release-notes"],
    "verification_command": "python -c \"from pathlib import Path; text=Path('RELEASE_NOTES.md').read_text(); required=('quoted fields','exit status','legacy --old'); assert all(item in text for item in required)\"",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_security_review_normal",
    "workspace": "workspaces/bundled_security_review",
    "task": "Perform a security assessment of proposed.diff and write severity-ordered findings with evidence and remediation to SECURITY_REPORT.md. Do not modify the source.",
    "invocation_mode": "normal",
    "expected_skills": ["security-review"],
    "verification_command": "python -c \"from pathlib import Path; text=Path('SECURITY_REPORT.md').read_text().lower(); source=Path('src/loader.py').read_text(); assert 'eval' in text and 'severity' in text and 'eval(payload)' in source\"",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_security_review_explicit",
    "workspace": "workspaces/bundled_security_review",
    "task": "Audit proposed.diff for exploitable risks, preserve the source unchanged, and record severity, evidence, and remediation in SECURITY_REPORT.md.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "security-review",
    "expected_skills": ["security-review"],
    "verification_command": "python -c \"from pathlib import Path; text=Path('SECURITY_REPORT.md').read_text().lower(); source=Path('src/loader.py').read_text(); assert 'eval' in text and 'severity' in text and 'eval(payload)' in source\"",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_skill_creator_normal",
    "workspace": "workspaces/bundled_skill_creator",
    "task": "Create this workspace's first reusable instruction bundle named log-triage through the supported Alysis lifecycle flow. It should guide investigation of structured application logs, and it must validate cleanly.",
    "invocation_mode": "normal",
    "expected_skills": ["skill-creator"],
    "verification_command": "python -c \"import subprocess,sys; from pathlib import Path; subprocess.run([sys.executable,'-m','alysis_code','skill','validate','.alysis_skills/log-triage'], check=True); assert Path('.alysis_skills/log-triage/SKILL.md').is_file()\"",
    "tags": ["bundled-pack", "positive"]
  },
  {
    "id": "bundled_skill_creator_explicit",
    "workspace": "workspaces/bundled_skill_creator",
    "task": "Scaffold and complete a reusable log-triage instruction bundle using the supported lifecycle commands, then validate its final bundle.",
    "invocation_mode": "explicit_skill",
    "explicit_skill_name": "skill-creator",
    "expected_skills": ["skill-creator"],
    "verification_command": "python -c \"import subprocess,sys; from pathlib import Path; subprocess.run([sys.executable,'-m','alysis_code','skill','validate','.alysis_skills/log-triage'], check=True); assert Path('.alysis_skills/log-triage/SKILL.md').is_file()\"",
    "tags": ["bundled-pack", "explicit"]
  },
  {
    "id": "bundled_negative_debug_concept",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Explain what a ValueError means in Python using only README.md. There is no failing program to investigate; do not run or change code.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('README.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "debug-adjacent"]
  },
  {
    "id": "bundled_negative_ci_summary",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Summarize the CI stages documented in CI.md. No check is failing, and no files should be changed.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('CI.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "fix-ci-adjacent"]
  },
  {
    "id": "bundled_negative_security_explanation",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Explain why parameterized queries are safer than string concatenation using README.md only. Do not audit this workspace or change files.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('README.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "security-review-adjacent"]
  },
  {
    "id": "bundled_negative_pr_discussion",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Using README.md only, explain how a general review discussion differs from an unresolved request that needs action. There is no pull request or review thread to address; do not inspect or change code.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('README.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "address-pr-comments-adjacent"]
  },
  {
    "id": "bundled_negative_code_review_concept",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Define code review for a newcomer using README.md only. Do not review this workspace, inspect a diff, or change files.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('README.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "code-review-adjacent"]
  },
  {
    "id": "bundled_negative_commit_concept",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Explain the purpose of Conventional Commits using README.md only. Do not stage files, create a commit, or change anything.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('README.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "commit-adjacent"]
  },
  {
    "id": "bundled_negative_release_notes_concept",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Explain what release notes usually contain using README.md only. Do not inspect Git history, draft project release notes, tag, publish, or change files.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('README.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "release-notes-adjacent"]
  },
  {
    "id": "bundled_negative_one_off_log_checklist",
    "workspace": "workspaces/bundled_negative_controls",
    "task": "Return a one-paragraph, one-off checklist for reading structured application logs, using README.md only. Do not create or update a reusable skill, bundle, or file.",
    "invocation_mode": "normal",
    "expected_skills": [],
    "verification_command": "python -c \"from pathlib import Path; assert Path('README.md').is_file()\"",
    "session_mode": "readonly",
    "tags": ["bundled-pack", "negative-control", "skill-creator-adjacent"]
  }
]
