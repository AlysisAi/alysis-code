# Configure a provider

Run **Configure Provider** to store your model provider API key in VS Code SecretStorage. The key
is passed to the CLI through the child process environment — never written to settings, command
lines, or logs.

From the same flow you can set or clear the lightweight router model used for routing calls.

The extension redacts secrets from any error or diagnostic output it shows.
