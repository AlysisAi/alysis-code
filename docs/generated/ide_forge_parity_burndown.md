# Forge CLI to VS Code parity burn-down

<!-- Generated by scripts/qa/check_ide_cli_parity.py. Do not edit by hand. -->

This Forge-specific report is generated from `docs/generated/ide_cli_parity_matrix.json` and the current static feature inventory. It projects the Forge command, Forge chat, IDE bridge, VS Code command, and slash surfaces into a compact release-review table.

## Summary

| Forge status | Feature count |
| --- | ---: |
| `implemented_in_extension` | 101 |
| `implemented_in_bridge_only` | 5 |
| `intentionally_cli_only` | 5 |
| `blocked_until_security_model` | 4 |
| `experimental_with_gate` | 1 |

## Swarm Gate

- `forge swarm` is `implemented_in_extension`.
- IDE bridge swarm job methods are advertised: cooperative checkpoint cancellation with preserved interrupted worktrees, a scheduler invariant guaranteeing disjoint concurrent write scopes, a swarm-layer write-scope dispatch guard, bounded redacted progress events with reconnect replay, and read-only reconcile with explicit harvest/discard actions.
- Advertised swarm methods: `forge.swarm.apply`, `forge.swarm.cancel`, `forge.swarm.discard`, `forge.swarm.list`, `forge.swarm.reconcile`, `forge.swarm.result`, `forge.swarm.resume`, `forge.swarm.review`, `forge.swarm.start`, `forge.swarm.status`

## Forge Features

| Status | Surface | Feature | Rationale |
| --- | --- | --- | --- |
| `blocked_until_security_model` | `cli_command` | `forge exec` | The broad CLI Forge exec command remains larger than IDE Forge Execute v1 review mode; IDE callers may pass explicit no_log and optional positive max_steps, but auto/fullaccess execution stays blocked until cancellation, sandbox, worker lifecycle, and approval guarantees are stronger. |
| `blocked_until_security_model` | `cli_command` | `forge resume` | forge resume continues an interrupted run by delegating to the sequential execution engine with retries enabled, so it inherits every constraint on broad CLI Forge execution. It also adds a decision of its own: when the plan drifted while the run was dead, --reapprove accepts work that was never approved. That approval belongs in typed IDE UI showing the drift, not in a terminal-scraped flag. |
| `blocked_until_security_model` | `cli_command` | `forge unlock` | forge unlock without --force only clears locks whose owner is provably gone, which is already automatic on acquisition and needs no IDE surface. The command exists for the ambiguous cases, and its one real power -- --force -- is what must not be a button until there is an approval surface for it. |
| `blocked_until_security_model` | `ide_capability` | `features.forge.execute.unsafe_modes` | IDE protocol intentionally blocks auto/fullaccess Forge execution and exposes no experimental flag by default. |
| `experimental_with_gate` | `cli_forge_chat_command` | `/execute` | Terminal Forge /execute plan drives full-plan parallel mutation. The IDE now exposes that as the capability-gated Run Swarm console (forgeSwarm gate + Workspace Trust), with cooperative cancellation, the disjoint-write-scope scheduler invariant, the swarm-layer write guard, non-modal approvals replacing --yes, and review-only merge (per-task Keep/Discard, never auto-merged). It is experimental-with-gate rather than fully implemented because broad auto/fullaccess execution and merge-to-base stay blocked: the IDE swarm is review-only and selected by capability, not an unscoped chat command. |
| `implemented_in_bridge_only` | `ide_bridge_method` | `forge.plan.regenerate.result` | Async job form of forge.plan.regenerate on the shared bridge job registry, mirroring forge.plan.start/forge.plan.result. The planner runs outside the bridge state lock (snapshot, compute, commit) with cooperative checkpoint cancellation via forge.cancel; the commit re-checks the persisted plan revision and fails closed with stale_plan_revision when a concurrent plan edit landed during regeneration. The typed bridge client covers both methods. |
| `implemented_in_bridge_only` | `ide_bridge_method` | `forge.plan.regenerate.start` | Async job form of forge.plan.regenerate on the shared bridge job registry, mirroring forge.plan.start/forge.plan.result. The planner runs outside the bridge state lock (snapshot, compute, commit) with cooperative checkpoint cancellation via forge.cancel; the commit re-checks the persisted plan revision and fails closed with stale_plan_revision when a concurrent plan edit landed during regeneration. The typed bridge client covers both methods. |
| `implemented_in_bridge_only` | `ide_bridge_method` | `forge.review.result` | Async job form of forge.review on the shared bridge job registry: the review provider call runs in a job thread instead of freezing the stdio dispatch loop, with cooperative cancellation checkpoints before and after the provider call and structured failure codes preserved. Sync forge.review remains for back-compat. |
| `implemented_in_bridge_only` | `ide_bridge_method` | `forge.review.start` | Async job form of forge.review on the shared bridge job registry: the review provider call runs in a job thread instead of freezing the stdio dispatch loop, with cooperative cancellation checkpoints before and after the provider call and structured failure codes preserved. Sync forge.review remains for back-compat. |
| `implemented_in_bridge_only` | `ide_bridge_method` | `forge.swarm.reconcile` | Post-crash/reload reconciliation: a read-only per-task report (merged / interrupted with diff-available / unstarted) over run artifacts and preserved worktrees, plus explicit idempotent harvest/discard actions. Typed-client covered (forgeSwarmReconcile) but not yet surfaced as a cockpit recovery flow β€” it backs harvest/discard and crash recovery beneath the Run Swarm console. |
| `implemented_in_extension` | `cli_command` | `forge assets add` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets cancel-pending` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets check-plan` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets delete` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets edit` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets list` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets prune-legacy` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets refresh` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge assets show` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge attach` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge plan` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge review` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge run` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge show` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge status` | These CLI workflows have typed VS Code command, Cockpit, slash, grouped backend-action, health, session, sandbox doctor, Forge, or management equivalents and do not require terminal-output scraping. |
| `implemented_in_extension` | `cli_command` | `forge swarm` | Forge swarm is now a first-class IDE workflow: the cockpit Run Swarm console (renderSwarmConsole) and the alysis.runSwarm command dispatch the active plan across parallel workers behind the forgeSwarm capability gate and Workspace Trust. Workers run without --yes auto-approval (dangerous actions raise non-modal task-attributed approvals); merge is review-only (per-task Keep/Discard via forge.swarm.apply/discard, never auto-merged, untracked-files surfaced); cancellation is cooperative; failed/interrupted tasks offer an editable regenerate-subtree recovery. Terminal forge swarm keeps --yes and auto-merge. |
| `implemented_in_extension` | `cli_forge_chat_command` | `/assets` | The extension exposes Forge help, plan display, and active-plan asset actions through typed Cockpit, Forge Plan, and backend-action routes. |
| `implemented_in_extension` | `cli_forge_chat_command` | `/assistant` | The VS Code extension exposes typed Forge plan-edit routes for assistant instruction, goal updates, and task show/update via forge.plan.getState, forge.plan.setAssistant, forge.plan.setGoal, and forge.plan.updateTask. Mutations are Workspace Trust gated and use optimistic plan revisions instead of terminal Forge chat state. |
| `implemented_in_extension` | `cli_forge_chat_command` | `/goal` | The VS Code extension exposes typed Forge plan-edit routes for assistant instruction, goal updates, and task show/update via forge.plan.getState, forge.plan.setAssistant, forge.plan.setGoal, and forge.plan.updateTask. Mutations are Workspace Trust gated and use optimistic plan revisions instead of terminal Forge chat state. |
| `implemented_in_extension` | `cli_forge_chat_command` | `/help` | The extension exposes Forge help, plan display, and active-plan asset actions through typed Cockpit, Forge Plan, and backend-action routes. |
| `implemented_in_extension` | `cli_forge_chat_command` | `/plan` | Forge-context /plan inspection and regeneration use typed Forge methods. Chat plan mode and global /plan are retired; new IDE plans are explicitly created through /forge plan. |
| `implemented_in_extension` | `cli_forge_chat_command` | `/show` | The extension exposes Forge help, plan display, and active-plan asset actions through typed Cockpit, Forge Plan, and backend-action routes. |
| `implemented_in_extension` | `cli_forge_chat_command` | `/task` | The VS Code extension exposes typed Forge plan-edit routes for assistant instruction, goal updates, and task show/update via forge.plan.getState, forge.plan.setAssistant, forge.plan.setGoal, and forge.plan.updateTask. Mutations are Workspace Trust gated and use optimistic plan revisions instead of terminal Forge chat state. |
| `implemented_in_extension` | `extension_chat_participant_command` | `forge` | The optional native VS Code chat participant is a thin router into the Cockpit and typed extension controllers. |
| `implemented_in_extension` | `extension_command` | `alysis.forgeExecute` | These command-palette entries are contributed by package.json and route to typed extension controllers or recovery helpers. alysis.locateCli locates, validates, and persists the Alysis Code CLI path safely. |
| `implemented_in_extension` | `extension_command` | `alysis.forgeListPlans` | These command-palette entries are contributed by package.json and route to typed extension controllers or recovery helpers. alysis.locateCli locates, validates, and persists the Alysis Code CLI path safely. |
| `implemented_in_extension` | `extension_command` | `alysis.forgeOpenPlan` | These command-palette entries are contributed by package.json and route to typed extension controllers or recovery helpers. alysis.locateCli locates, validates, and persists the Alysis Code CLI path safely. |
| `implemented_in_extension` | `extension_command` | `alysis.forgePlan` | These command-palette entries are contributed by package.json and route to typed extension controllers or recovery helpers. alysis.locateCli locates, validates, and persists the Alysis Code CLI path safely. |
| `implemented_in_extension` | `extension_command` | `alysis.manageForgeAssets` | These command-palette entries are contributed by package.json and route to typed extension controllers or recovery helpers. alysis.locateCli locates, validates, and persists the Alysis Code CLI path safely. |
| `implemented_in_extension` | `extension_command` | `alysis.runSwarm` | These command-palette entries are contributed by package.json and route to typed extension controllers or recovery helpers. alysis.locateCli locates, validates, and persists the Alysis Code CLI path safely. |
| `implemented_in_extension` | `extension_command` | `alysis.showForge` | These command-palette entries are contributed by package.json and route to typed extension controllers or recovery helpers. alysis.locateCli locates, validates, and persists the Alysis Code CLI path safely. |
| `implemented_in_extension` | `extension_slash_alias` | `/execute` | These aliases are registered in SlashCommandRegistry.ts and route only to the existing Forge Execute Review path. Broad execute arguments return a structured IDE v1 warning; auto/fullaccess execution remains blocked. |
| `implemented_in_extension` | `extension_slash_alias` | `/forge exec` | These aliases are registered in SlashCommandRegistry.ts and route only to the existing Forge Execute Review path. Broad execute arguments return a structured IDE v1 warning; auto/fullaccess execution remains blocked. |
| `implemented_in_extension` | `extension_slash_alias` | `/forge execute` | These aliases are registered in SlashCommandRegistry.ts and route only to the existing Forge Execute Review path. Broad execute arguments return a structured IDE v1 warning; auto/fullaccess execution remains blocked. |
| `implemented_in_extension` | `extension_slash_command` | `/artifacts` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset add` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset cancel-pending` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset check` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset delete` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset edit` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset list` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset prune` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset refresh` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/asset show` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/assets` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/assistant` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/diffs` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/execute plan` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/execute preview` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/forge plan` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/forge plan regenerate` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/forge plan show` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/forge plan state` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/forge plan validate` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/forge review` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/forge show` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/goal` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/plans` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/review` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `extension_slash_command` | `/task` | These webview slash commands are registered in SlashCommandRegistry.ts and route through extension controllers, not terminal CLI parsing. |
| `implemented_in_extension` | `ide_bridge_method` | `artifact.list` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `artifact.read` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `diff.get` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `diff.list` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.add` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.cancelPending` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.checkPlan` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.delete` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.edit` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.list` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.pruneLegacy` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.refresh` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.assets.show` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.attach` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.cancel` | The method is implemented as cooperative checkpoint cancellation for active Forge jobs, advertised through features.forge.cancel.supported, and routed by the VS Code Cancel Current Run command without relying on method presence alone. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.execute` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.executePreview` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.list` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.open` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.getState` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.regenerate` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.result` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.setAssistant` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.setGoal` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.start` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.updateTask` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.plan.validate` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.resume` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.review` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.show` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.status` | These IDE Protocol v1 methods are documented, advertised by health.py, and consumed by VS Code feature gates, controllers, or typed bridge-client methods. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.apply` | The cockpit Run Swarm console (renderSwarmConsole + alysis.runSwarm) routes these typed bridge methods: forge.swarm.start creates an idempotent durable job and runs the swarm engine behind Workspace Trust with cooperative cancellation; the task grid renders swarm_worker_state_changed progress by glyph/weight; non-modal task-attributed approvals replace --yes (workers stay paused, never auto-denied); forge.swarm.review lists per-task diffs (with the untracked-files-not-applied list surfaced) which the user keeps via forge.swarm.apply (working tree only, never commits) or drops via forge.swarm.discard; failed/interrupted tasks offer an editable regenerate-subtree recovery. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.cancel` | The cockpit Run Swarm console (renderSwarmConsole + alysis.runSwarm) routes these typed bridge methods: forge.swarm.start creates an idempotent durable job and runs the swarm engine behind Workspace Trust with cooperative cancellation; the task grid renders swarm_worker_state_changed progress by glyph/weight; non-modal task-attributed approvals replace --yes (workers stay paused, never auto-denied); forge.swarm.review lists per-task diffs (with the untracked-files-not-applied list surfaced) which the user keeps via forge.swarm.apply (working tree only, never commits) or drops via forge.swarm.discard; failed/interrupted tasks offer an editable regenerate-subtree recovery. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.discard` | The cockpit Run Swarm console (renderSwarmConsole + alysis.runSwarm) routes these typed bridge methods: forge.swarm.start creates an idempotent durable job and runs the swarm engine behind Workspace Trust with cooperative cancellation; the task grid renders swarm_worker_state_changed progress by glyph/weight; non-modal task-attributed approvals replace --yes (workers stay paused, never auto-denied); forge.swarm.review lists per-task diffs (with the untracked-files-not-applied list surfaced) which the user keeps via forge.swarm.apply (working tree only, never commits) or drops via forge.swarm.discard; failed/interrupted tasks offer an editable regenerate-subtree recovery. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.list` | The Forge Cockpit exposes a bounded durable recovery picker backed by workspace/session-scoped list and revision-fenced resume calls. Resume requires Workspace Trust and a fresh explicit confirmation, never reuses prior grants, cancels superseded local ownership safely, and keeps interrupted task worktrees isolated for review. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.result` | The cockpit Run Swarm console (renderSwarmConsole + alysis.runSwarm) routes these typed bridge methods: forge.swarm.start creates an idempotent durable job and runs the swarm engine behind Workspace Trust with cooperative cancellation; the task grid renders swarm_worker_state_changed progress by glyph/weight; non-modal task-attributed approvals replace --yes (workers stay paused, never auto-denied); forge.swarm.review lists per-task diffs (with the untracked-files-not-applied list surfaced) which the user keeps via forge.swarm.apply (working tree only, never commits) or drops via forge.swarm.discard; failed/interrupted tasks offer an editable regenerate-subtree recovery. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.resume` | The Forge Cockpit exposes a bounded durable recovery picker backed by workspace/session-scoped list and revision-fenced resume calls. Resume requires Workspace Trust and a fresh explicit confirmation, never reuses prior grants, cancels superseded local ownership safely, and keeps interrupted task worktrees isolated for review. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.review` | The cockpit Run Swarm console (renderSwarmConsole + alysis.runSwarm) routes these typed bridge methods: forge.swarm.start creates an idempotent durable job and runs the swarm engine behind Workspace Trust with cooperative cancellation; the task grid renders swarm_worker_state_changed progress by glyph/weight; non-modal task-attributed approvals replace --yes (workers stay paused, never auto-denied); forge.swarm.review lists per-task diffs (with the untracked-files-not-applied list surfaced) which the user keeps via forge.swarm.apply (working tree only, never commits) or drops via forge.swarm.discard; failed/interrupted tasks offer an editable regenerate-subtree recovery. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.start` | The cockpit Run Swarm console (renderSwarmConsole + alysis.runSwarm) routes these typed bridge methods: forge.swarm.start creates an idempotent durable job and runs the swarm engine behind Workspace Trust with cooperative cancellation; the task grid renders swarm_worker_state_changed progress by glyph/weight; non-modal task-attributed approvals replace --yes (workers stay paused, never auto-denied); forge.swarm.review lists per-task diffs (with the untracked-files-not-applied list surfaced) which the user keeps via forge.swarm.apply (working tree only, never commits) or drops via forge.swarm.discard; failed/interrupted tasks offer an editable regenerate-subtree recovery. |
| `implemented_in_extension` | `ide_bridge_method` | `forge.swarm.status` | The cockpit Run Swarm console (renderSwarmConsole + alysis.runSwarm) routes these typed bridge methods: forge.swarm.start creates an idempotent durable job and runs the swarm engine behind Workspace Trust with cooperative cancellation; the task grid renders swarm_worker_state_changed progress by glyph/weight; non-modal task-attributed approvals replace --yes (workers stay paused, never auto-denied); forge.swarm.review lists per-task diffs (with the untracked-files-not-applied list surfaced) which the user keeps via forge.swarm.apply (working tree only, never commits) or drops via forge.swarm.discard; failed/interrupted tasks offer an editable regenerate-subtree recovery. |
| `intentionally_cli_only` | `cli_chat_slash_command` | `/forge` | These terminal navigation, quick-panel, active-child picker, and toolbar affordances use native IDE surfaces. The IDE /subagents status|on|off route exposes typed delegation configuration; the CLI active-child monitor remains a terminal surface. |
| `intentionally_cli_only` | `cli_command_group` | `forge` | Typer group namespaces are tracked so new groups cannot appear silently; concrete subcommands and IDE methods carry the actual parity decision. |
| `intentionally_cli_only` | `cli_command_group` | `forge assets` | Typer group namespaces are tracked so new groups cannot appear silently; concrete subcommands and IDE methods carry the actual parity decision. |
| `intentionally_cli_only` | `cli_forge_chat_command` | `/back` | These are terminal Forge mode navigation/finalization commands; the IDE uses persistent typed plan state and native view lifecycle. |
| `intentionally_cli_only` | `cli_forge_chat_command` | `/done` | These are terminal Forge mode navigation/finalization commands; the IDE uses persistent typed plan state and native view lifecycle. |
